/*
───────────────────────────────────────────────────────────────
📘 Problem: 2654. Minimum Number of Operations to Make All Array Elements Equal to 1
💡 Difficulty: Medium
🧠 Topics: Array, Math, GCD, Number Theory
🔗 Link: https://leetcode.com/problems/minimum-number-of-operations-to-make-all-array-elements-equal-to-1/
───────────────────────────────────────────────────────────────
🧩 Problem Statement:

You are given an array `nums` consisting of positive integers.

You can perform the following operation any number of times:
- Select an index `i` such that 0 ≤ i < n - 1,  
  and replace either `nums[i]` or `nums[i + 1]` with their **GCD** value.

Return the **minimum number of operations** needed to make all elements equal to 1.  
If it’s impossible, return `-1`.

───────────────────────────────────────────────────────────────
🔒 Constraints:
• 2 ≤ nums.length ≤ 50  
• 1 ≤ nums[i] ≤ 10⁶  
───────────────────────────────────────────────────────────────
*/


/*
───────────────────────────────────────────────────────────────
🥇 Approach — GCD Reduction + Greedy Observation
───────────────────────────────────────────────────────────────
💡 Idea Breakdown:

The key insight is that **GCD operations propagate "1"s** through the array.

1️⃣ **If any element is already 1:**
   - Each operation can turn one other number into 1 by pairing with an existing 1.
   - Hence, we need exactly `(n - count(1))` operations.

2️⃣ **If no 1 exists:**
   - We must *first create a 1* by taking GCDs across adjacent elements.
   - To do that, we search for the **smallest subarray** whose GCD = 1.
   - If such a subarray exists of length `L`, then:
     - It takes `(L - 1)` operations to form the first 1.
     - Then `(n - 1)` operations to spread that 1 across the rest.
     - Total operations = `(n - 1) + (L - 1)`.

3️⃣ **If no subarray has GCD = 1:**
   - It is impossible → return `-1`.

───────────────────────────────────────────────────────────────
🔹 Example 1:
nums = [2,6,3,4]
→ GCD subarray [3,4] = 1 → L = 2
→ Total = (n - 1) + (L - 1) = (4 - 1) + (2 - 1) = 4 ✅

───────────────────────────────────────────────────────────────
🔹 Example 2:
nums = [2,10,6,14]
→ No subarray has GCD = 1 → impossible → -1 ❌

───────────────────────────────────────────────────────────────
🧮 Complexity:
• Time:  O(n² * log(max(nums[i])))  
• Space: O(1)
───────────────────────────────────────────────────────────────
✅ Efficient for n ≤ 50, as required by constraints.
───────────────────────────────────────────────────────────────
*/


public class MinimumOperationsToMakeAllArrayElementsEqualTo1 {
    public int minOperations(int[] nums) {
        int n = nums.length;
        int count1 = 0;

        // Step 1️⃣ — Count how many 1s already exist
        for (int x : nums) {
            if (x == 1) count1++;
        }

        // Case 1: There are already 1's → need (n - count1) operations
        if (count1 != 0) return n - count1;

        // Step 2️⃣ — Find smallest subarray whose GCD == 1
        int minLen = Integer.MAX_VALUE;
        for (int i = 0; i < n; i++) {
            int g = nums[i];
            for (int j = i + 1; j < n; j++) {
                g = gcd(g, nums[j]);
                if (g == 1) {
                    minLen = Math.min(minLen, j - i + 1);
                    break; // stop early when GCD becomes 1
                }
            }
        }

        // Step 3️⃣ — If impossible
        if (minLen == Integer.MAX_VALUE) return -1;

        // Step 4️⃣ — Total operations = (n - 1) to spread + (minLen - 1) to form 1
        return (n - 1) + (minLen - 1);
    }

    // ───────────────────────────────────────────────────────────────
    // Helper: Compute GCD using Euclidean algorithm
    // ───────────────────────────────────────────────────────────────
    private int gcd(int a, int b) {
        while (b != 0) {
            int temp = a % b;
            a = b;
            b = temp;
        }
        return a;
    }
}


/*
───────────────────────────────────────────────────────────────
🧩 Example Walkthroughs:
───────────────────────────────────────────────────────────────
Example 1:
nums = [2, 6, 3, 4]
→ GCD([3,4]) = 1 → L = 2
→ Total ops = (n - 1) + (L - 1) = 3 + 1 = 4
✅ Output: 4

Example 2:
nums = [2, 10, 6, 14]
→ No subarray has GCD = 1
✅ Output: -1
───────────────────────────────────────────────────────────────
*/


/*
───────────────────────────────────────────────────────────────
🧮 Final Complexity Summary:
───────────────────────────────────────────────────────────────
Time:  O(n² * log(max(nums[i])))
Space: O(1)
───────────────────────────────────────────────────────────────
✅ Final Choice → GCD-based greedy with subarray search
───────────────────────────────────────────────────────────────
*/
