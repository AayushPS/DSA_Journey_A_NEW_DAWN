/*
───────────────────────────────────────────────────────────────
📘 Problem: 1669. Merge In Between Linked Lists
💡 Difficulty: Medium
🧠 Topics: Linked List, Two Pointers, Simulation
🔗 Link: https://leetcode.com/problems/merge-in-between-linked-lists/
───────────────────────────────────────────────────────────────
🧩 Problem Statement:

You are given two linked lists `list1` and `list2` of sizes `n` and `m` respectively,
along with two integers `a` and `b`.

You must remove the nodes from index `a` to `b` (inclusive) from `list1`
and insert the entire `list2` in their place.

Return the head of the modified list.

───────────────────────────────────────────────────────────────
🔒 Constraints:
• 3 ≤ list1.length ≤ 10⁴  
• 1 ≤ a ≤ b < list1.length - 1  
• 1 ≤ list2.length ≤ 10⁴  
───────────────────────────────────────────────────────────────
*/


/*
───────────────────────────────────────────────────────────────
🥇 Approach — Pointer Splicing (Iterative Traversal)
───────────────────────────────────────────────────────────────
💡 Idea Breakdown:

1️⃣ Traverse `list1` to find:
   - `nodeA` → the node just **before index a**  
   - `nodeB` → the node **just after index b**

2️⃣ Connect `nodeA.next` to the head of `list2`.

3️⃣ Traverse to the end of `list2` to find its tail.

4️⃣ Connect that tail’s `.next` to `nodeB`.

✅ This cleanly merges the two lists in-place without extra space.

───────────────────────────────────────────────────────────────
🔹 Example:
Input:
list1 = [10,1,13,6,9,5], a = 3, b = 4, list2 = [1000000,1000001,1000002]

Process:
Step 1 → Find node before index 3 (value 13) → nodeA  
Step 2 → Find node after index 4 (value 5) → nodeB  
Step 3 → Connect nodeA.next = list2  
Step 4 → Connect tail(list2).next = nodeB

✅ Output → [10,1,13,1000000,1000001,1000002,5]

───────────────────────────────────────────────────────────────
🧮 Complexity:
• Time: O(n + m) — traverse both lists once  
• Space: O(1) — constant extra space  
───────────────────────────────────────────────────────────────
✅ Simple and optimal pointer manipulation solution.
───────────────────────────────────────────────────────────────
*/

/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode mergeInBetween(ListNode list1, int a, int b, ListNode list2) {
        ListNode temp1 = list1;

        // Step 1️⃣ — Find the node before index 'a'
        while (a != 1) {
            a--;
            b--;
            temp1 = temp1.next;
        }

        // Step 2️⃣ — Find the node after index 'b'
        ListNode temp2 = temp1;
        while (b != -1) {
            temp2 = temp2.next;
            b--;
        }

        // Step 3️⃣ — Connect list1’s nodeA to head of list2
        temp1.next = list2;

        // Step 4️⃣ — Move to tail of list2
        while (temp1.next != null) {
            temp1 = temp1.next;
        }

        // Step 5️⃣ — Connect tail of list2 to nodeB
        temp1.next = temp2;

        return list1;
    }
}


/*
───────────────────────────────────────────────────────────────
🧩 Example Walkthroughs:
───────────────────────────────────────────────────────────────
Example 1:
list1 = [10,1,13,6,9,5]
a = 3, b = 4
list2 = [1000000,1000001,1000002]

→ Remove indices [3,4] → remove [6,9]
→ Insert list2
✅ Output: [10,1,13,1000000,1000001,1000002,5]

Example 2:
list1 = [0,1,2,3,4,5,6]
a = 2, b = 5
list2 = [1000000,1000001,1000002,1000003,1000004]

→ Remove [2..5] → remove [2,3,4,5]
→ Insert list2
✅ Output: [0,1,1000000,1000001,1000002,1000003,1000004,6]
───────────────────────────────────────────────────────────────
*/


/*
───────────────────────────────────────────────────────────────
🧮 Final Complexity Summary:
───────────────────────────────────────────────────────────────
Time:  O(n + m)
Space: O(1)
───────────────────────────────────────────────────────────────
✅ Final Choice → Two-pointer splicing (in-place merge)
───────────────────────────────────────────────────────────────
*/
