/*
───────────────────────────────────────────────────────────────
📘 Problem: 1721. Swapping Nodes in a Linked List
💡 Difficulty: Medium
🧠 Topics: Linked List, Two Pointers
🔗 Link: https://leetcode.com/problems/swapping-nodes-in-a-linked-list/
───────────────────────────────────────────────────────────────
🧩 Problem Statement:

You are given the head of a singly linked list and an integer k.

Return the head of the linked list after **swapping the values** of:
- the kᵗʰ node from the **beginning**
- and the kᵗʰ node from the **end**

The list is **1-indexed**.

───────────────────────────────────────────────────────────────
🔒 Constraints:
• 1 ≤ k ≤ n ≤ 10⁵  
• 0 ≤ Node.val ≤ 100
───────────────────────────────────────────────────────────────
*/


/*
───────────────────────────────────────────────────────────────
🥇 Approach — Two Pointers (Single Pass After Finding kᵗʰ Node)
───────────────────────────────────────────────────────────────
💡 Idea Breakdown:

We want to swap the **kᵗʰ node from the start** and the **kᵗʰ node from the end**.

1️⃣ Move one pointer `p` forward (k-1) times to locate the **kᵗʰ node from start**.  
   Store it as `firstNode`.

2️⃣ Now use another pointer `temp2 = head` and move both `p` and `temp2`
   together until `p` reaches the end.
   - When `p` is at the last node, `temp2` will be at the **kᵗʰ node from the end**.
   - Store this as `secondNode`.

3️⃣ Swap their values.

⚠️ Note: We swap **values**, not actual nodes — keeps the structure intact.

───────────────────────────────────────────────────────────────
🔹 Example:
Input: head = [1, 2, 3, 4, 5], k = 2

Step 1 → 2nd node from start = 2  
Step 2 → 2nd node from end = 4  
Step 3 → Swap values (2 ↔ 4) → [1, 4, 3, 2, 5]

✅ Output: [1, 4, 3, 2, 5]

───────────────────────────────────────────────────────────────
🧮 Complexity:
• Time:  O(n) → single traversal of list  
• Space: O(1) → no extra data structures
───────────────────────────────────────────────────────────────
✅ Clean and efficient pointer manipulation solution.
───────────────────────────────────────────────────────────────
*/

/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */

class Solution {
    public ListNode swapNodes(ListNode head, int k) {
        ListNode p = head;

        // Step 1️⃣ — Find the kᵗʰ node from the start
        while (--k > 0) {
            p = p.next;
        }
        ListNode firstNode = p;

        // Step 2️⃣ — Find the kᵗʰ node from the end
        ListNode secondNode = head;
        while (p.next != null) {
            secondNode = secondNode.next;
            p = p.next;
        }

        // Step 3️⃣ — Swap their values
        int temp = firstNode.val;
        firstNode.val = secondNode.val;
        secondNode.val = temp;

        return head;
    }
}


/*
───────────────────────────────────────────────────────────────
🧩 Example Walkthroughs:
───────────────────────────────────────────────────────────────
Example 1:
Input:  [1, 2, 3, 4, 5], k = 2
Step 1 → 2nd node from start = 2
Step 2 → 2nd node from end = 4
Step 3 → Swap → [1, 4, 3, 2, 5]
✅ Output: [1, 4, 3, 2, 5]

Example 2:
Input:  [7,9,6,6,7,8,3,0,9,5], k = 5
Step 1 → 5th from start = 7
Step 2 → 5th from end = 8
Step 3 → Swap → [7,9,6,6,8,7,3,0,9,5]
✅ Output: [7,9,6,6,8,7,3,0,9,5]
───────────────────────────────────────────────────────────────
*/


/*
───────────────────────────────────────────────────────────────
🧮 Final Complexity Summary:
───────────────────────────────────────────────────────────────
Time:  O(n)
Space: O(1)
───────────────────────────────────────────────────────────────
✅ Final Choice → Two-pointer traversal with in-place value swap.
───────────────────────────────────────────────────────────────
*/
